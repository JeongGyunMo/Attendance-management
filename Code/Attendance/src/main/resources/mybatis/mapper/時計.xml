<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="jp.ac.mappers.時計mapper">
	<select id="Clocksave" resultType="String">
		INSERT INTO times(employeeid , attendtime)
		VALUES(123,#{Clock})
	</select>
</mapper>