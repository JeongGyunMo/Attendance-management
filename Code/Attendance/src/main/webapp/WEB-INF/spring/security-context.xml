<?xml version="1.0" encoding="UTF-8"?>
<beans:beans
	xmlns="http://www.springframework.org/schema/security"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
http://www.springframework.org/schema/security
http://www.springframework.org/schema/security/spring-security-4.2.xsd">
	<http auto-config='true' use-expressions="true">
		<intercept-url pattern="/favicon.ico"
			access="hasRole('ROLE_ANONYMOUS')" />
		<intercept-url pattern="/resources/**"
			access="permitAll" />
		<intercept-url pattern="/login/**" access="permitAll" />
		<intercept-url pattern="/**"
			access="hasRole('ROLE_ADMIN')" />
		<form-login login-page="/login/loginPage.do"
			login-processing-url="/login.do"
			authentication-failure-url="/login/loginPage.do?err=true"
			default-target-url="/" username-parameter="userid"
			password-parameter="userpw" />
		<session-management>
			<concurrency-control max-sessions="1"
				expired-url="/" />
		</session-management>
		<access-denied-handler
			error-page="/login/accessDenied.do" />
	</http>
	<authentication-manager>
		<authentication-provider>
			<!-- <user-service> <user name="admin" password="1234" authorities="ROLE_USER, 
				ROLE_ADMIN" /> <user name="guest" password="1234" authorities="ROLE_USER" 
				/> <user name="guest2" password="a1234" authorities="ROLE_USER" /> </user-service> -->
			<!-- root-context.xml에서 설정한 bean id를 가져온다. -->
			<!-- 사용자 인증을 가져온다. username, password, enabled 컬럼은 필수 -->
			<!-- authorities-by-username-query : 권한 인증을 가져온다. Authority 필드 값은 "ROLE_ADMIN" 이나 "ROLE_USER"가 오도록 한다. -->
			<!-- TEST_USER 테이블 명 -->
			<jdbc-user-service data-source-ref="dataSource"
				users-by-username-query="SELECT EMPLOYEE_ID as id,
										PASSWORD,
										1 as enabled
										FROM EMPLOYEE WHERE EMPLOYEE=?"
				authorities-by-username-query="SELECT EMPLOYEE_ID as id,
				CASE WHEN GRADE='01' THEN 'ROLE_ADMIN'
				ELSE 'ROLE_USER'
				END authority
				FROM EMPLOYEE WHERE USERID=?" />
		</authentication-provider>
	</authentication-manager>
</beans:beans>